# Foto-placeholders

Deze map bevat tijdelijke placeholder-bestanden. Run vóór deploy:

```bash
./scripts/setup-photos.sh
```

Dat plaatst de echte 46 foto's van Van Wonderen Tegelwerken hier (uit ~/Downloads ZIP of via live download).
